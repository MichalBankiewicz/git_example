1. Git basic commands
       - git init
       - git status
       - git add
       - git rm --cached
       - git commit
       - git commit --ammend
       - git log
       - git checkout -- book.txt
       - git diff
       - git reset HEAD book.txt
       - git stash
       - git checkout -b branch_name
       - git stash pop --index 0
       - git checkout master
       - git merge (branch_name || origin/branch_name)
       - git branch -d branch_name
       - git branch    
       - git remote
       - git remote -v
       - git remote add (origin || dupa) remote_adresses_url
       - git remote rm (origin || dupa)
       - git pull origin master
       - git push origin master (--force) (!!)
       - git push origin -u master (upstream set - first push)


