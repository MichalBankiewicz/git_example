Contents list

1. Git basic commands
        - git init
        - git status
        - git add
        - git rm --cached
        - git commit
        - git commie --amend  /// zmiana nazwy commit ctrl+o ctrl +x
        - git log
	- git merge (branch_name || origin/branch_name) 	
	- git checkout -- book.txt
	- git diff
	- git reset HEAD book.txt
	- git stash
	- git checkout -b branch_name
	- git stash pop --index 0
	- git checkout master
	- git merge (branch_name || origin/branch_name)
	- git branch -d branch_name
	- git branch
	- git remote
	- git remote -v
	- git remote add
	- git remote add (origin || dupa) remote_address_url
	- git remote rm (origin || dupa)
